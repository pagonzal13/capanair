# Supabase (Project Settings -> API)
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# Service role key: SECRETO. Nunca lo expongas al cliente. Solo se usa en el servidor.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# Contraseña de acceso al sitio (invitados)
SITE_PASSWORD=capafest2026

# Contraseña del panel de administración (ruta /admin, solo para el organizador)
ADMIN_PASSWORD=admin2026

# Secreto usado para firmar las cookies de sesion (genera uno propio y unico)
# Puedes generarlo con: openssl rand -hex 32
SESSION_SECRET=change-this-to-a-long-random-string
